services:
  first-brush:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: first-brush-app
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - PARENT_PIN=1234
    volumes:
      - ../data:/app/data
