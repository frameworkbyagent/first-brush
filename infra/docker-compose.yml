services:
  first-brush:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: first-brush-app
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - ../data:/app/data
